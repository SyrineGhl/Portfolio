import React from 'react';

const Projects: React.FC = () => {
  return (
    <section id="projects">
      <h2>Mes Projets</h2>
      {/* Écris ici la liste de tes projets avec des liens vers leur démo ou leur code source. */}
    </section>
  );
};

export default Projects;
